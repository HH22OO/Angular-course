import { SucessAlertDirective } from './sucess-alert.directive';

describe('SucessAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new SucessAlertDirective();
    expect(directive).toBeTruthy();
  });
});
